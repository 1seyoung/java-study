package Algo.basic.subset;

public class BasicSubset {
    static int[] src = {1,2,3,4,5};
    //tgt 배열 x <= 정해진 수의 선택을 하는 것이 아니라, 가능한 모든 경우를 선택
    // 조합은 src, tgt 맨 앞부터 선택/비선택을 해가면서 tgt를 채우면 끝이지만
    // 부분집합은 src의 맹 앞부터 선택/비선택을 끝까지 계속한다.
 }
